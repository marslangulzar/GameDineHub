@model IEnumerable<BusinessEntities.Entities.Game.TeamSongVM>
@{
    ViewBag.Title = "Display";
    Layout = "~/Views/Shared/_LayoutDisplay.cshtml";
}
@*<h1 class="text-white" style="font-size:130px; font-weight:bolder; text-align:center">
        @{
            <span id="publishThemeCastText">@(TempData["ThemeCast"] == null? "Game Dine Hub": @TempData["ThemeCast"]) </span>
        }
    </h1>*@
<div id="winingBg">
    <div class="row"style="overflow-y:auto">
        <div class="col-lg-12 col-xs-12 col-md-12 col-sm-12 @( ViewBag.VotingRoundStatus==true? "":"d-none")" id="votingViewDisplay">
            <div class="alert bg-primary-200 text-white text-center text-lg-center fw-900 alertText" role="alert" id="display-alert">
                @{
                    <strong id="display-alert-message"> @(TempData["ThemeCast"] == null? "Game Dine Hub": @TempData["ThemeCast"]) </strong>
                }

            </div>

            <div id="votingDisplaySection" style="max-height:70vh; overflow-y:auto">
                @{ Html.RenderPartial("_CastVotingViewScreen", Model); }
            </div>
        </div>
        <div class="col-lg-12 col-xs-10 col-md-12 col-sm-12 @( ViewBag.VotingRoundStatus==true? "d-none":"")" id="castViewDisplay">
            <h1 class="text-white displaymessage anim-box">
                <p class="anim-h">
                    @{
                        <span id="publishThemeCastText">@(TempData["ThemeCast"] == null? "Game Dine Hub": @TempData["ThemeCast"]) </span>
                    }
                </p>
                <p class="anim-h">
                    @{
                        <span id="publishThemeCastText">@(TempData["ThemeCast"] == null? "Game Dine Hub": @TempData["ThemeCast"]) </span>
                    }
                </p>
            </h1>
        </div>
    </div>
</div>
<script type="text/javascript">

    function refreshCastVotingGrid() {
        try {

            var requestParams = {};
            var pageUrl = "/General/GetVotingSongForDisplay";
            ilms.partialPageLoadingToElement(pageUrl, requestParams, 'votingDisplaySection');
        } catch (e) {
        }
    }
    //$(function () {
    //    $('.scroll').jscroll({
    //        autoTrigger: true
    //    });
    //});

</script>

